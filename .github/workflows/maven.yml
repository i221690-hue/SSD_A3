name: Java CI - Secure Lab

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Show repo tree
        run: |
          pwd
          ls -la
          echo "find results:"
          find . -maxdepth 5 -name pom.xml -print || true

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Build with Maven
        run: mvn -U -B package --file pom.xml

      - name: Run tests
        env:
          JWT_SECRET: test-secret-test-secret-test-secret
          JWT_ISSUER: owasp-lab
          JWT_AUD: owasp-audience
        run: mvn test
